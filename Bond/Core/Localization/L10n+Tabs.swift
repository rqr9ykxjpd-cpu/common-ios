import Foundation

extension L10n {
    enum Tabs {
        static var feed: String { String(localized: "tabs.feed") }
        static var discover: String { String(localized: "tabs.discover") }
        static var profile: String { String(localized: "tabs.profile") }
    }
}
