import Foundation

extension L10n {
    enum Tier {
        static var free: String { String(localized: "tier.free") }
        static var plus: String { String(localized: "tier.plus") }
        static var pro: String { String(localized: "tier.pro") }
    }
}
